info:
  name: Refresh Token No Auth
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/users/refresh-token"
  headers:
    - name: accept
      value: application/json
  body:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 401", function() {
          expect(res.status).to.equal(401);
        });
        test("error code is invalid_token", function() {
          expect(res.body.code).to.equal("invalid_token");
        });

settings:
  encodeUrl: true

docs: |-
  Refresh sem header `Authorization`.

  **Espera:** 401 com `code: invalid_token`.
